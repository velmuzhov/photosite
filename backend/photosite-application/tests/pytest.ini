[tool:pytest]
testpaths = tests
python_files = tests/**/*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --asyncio-mode=auto
    --cov=app
    --cov-report=html
    --cov-report=term
    --cov-fail-under=80
